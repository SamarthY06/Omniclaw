# Secrets
omniclaw/.env
*.env

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
# Allow the Android release archive directory to be tracked (its own
# .gitignore inside the folder still excludes the .apk binaries).
!android/dist/
build/
.eggs/
*.egg

# SQLite databases (local state)
omniclaw/data/*.db

# macOS
.DS_Store
._*
*.app/

# Swift / Xcode
.build/
.swiftpm/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Virtual environments
venv/
.venv/
env/

# Screenshots (temp)
/tmp/omniclaw_*

# Android build outputs and downloaded toolchains (heavy, regenerable)
android/.gradle/
android/.gradle-toolchain/
android/app/build/
android/app/libnode/
android/app/.cxx/
android/local.properties

# Embedded Node deps (regenerated by `npm install` during bootstrap)
android/app/src/main/assets/node/node_modules/

# pytest cache + coverage artefacts
.pytest_cache/
.coverage
htmlcov/

# Editor / OS
*.log
