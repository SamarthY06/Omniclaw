# USER.md - what Ben knows about you

(Filled in over time as Ben learns. The agent maintains this file via the
`memory` tool family. Initial template only.)

- Name: (set this in Settings)
- Default device for desktop tasks: Mac (paired)
- Default device for mobile tasks: this phone
- Wake phrase: configured per Settings (default: "Ben")
